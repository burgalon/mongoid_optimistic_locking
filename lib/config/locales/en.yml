en:
  mongoid:
    errors:
      messages:
        stale_document:
          update:
            "Attempted to update a stale document: %{klass}"
          destroy:
            "Attempted to destroy a stale document: %{klass}"
